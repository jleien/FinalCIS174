@model PlayerListViewModel
@{
    ViewData["Title"] = "Player Looker";
}
<div class="row">
    <div class="col-sm-3">
        <h3 class="mt-3">Category</h3>
        <div class="list-group">
            @foreach (Class cla in Model.Classes)
            {
                <a asp-action="Index"
               asp-route-activeCat="@cla.ClassID"
               asp-route-activeGame="@Model.ActiveClass"
               class="list-group-item
                       @Model.CheckActiveClass(cla.ClassID)">@cla.Name</a>
            }
        </div>
        <h3 class="mt-3">Game</h3>
        <div class="list-group">
            @foreach (Race rac in Model.Races)
            {
                <a asp-action="Index"
               asp-route-activeCat="@Model.ActiveRace"
               asp-route-activeGame="@rac.RaceID"
               class="list-group-item
                       @Model.CheckActiveRace(rac.RaceID)">@rac.Name</a>
            }
        </div>
    </div>
    <div class="col-sm-9">
        <ul class="list-inline">
            @foreach (Player player in Model.Players)
            {
                <li class="list-inline-item">
                    <a asp-action="Details" asp-route-id="@player.PlayerID">
                        <img src="~/images/@player.Name" alt="@player.Name"
                         title="@player.Name | @player.Class.Name" />
                    </a>
                </li>
            }
        </ul>
    </div>
</div>